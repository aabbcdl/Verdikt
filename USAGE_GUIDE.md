# Verdikt 使用指南

这份指南只讲怎么把 Verdikt 跑起来、怎么判断结果、失败后该点哪里。

## 快速跑通示例

### 1. 安装并启动

```bash
cd D:\project\Verdikt
pnpm install
pnpm app
```

`pnpm app` 会先构建项目，然后启动控制台，并自动打开浏览器。

如果你只想启动服务、不想自动打开浏览器：

```bash
pnpm app:no-open
```

Windows 用户也可以用一键脚本：

```powershell
.\scripts\start-verdikt-app.ps1
```

### 2. 跑内置示例

浏览器打开后，按这个顺序点：

1. **填入示例**
2. **预览配置**
3. **开始执行任务**
4. 等待结束
5. 查看 **查看修改** 和 **查看完整结果**

示例项目里有一个故意写错的 `sum` 函数。Verdikt 会让执行 agent 修它，再让验收命令和审查 agent 一起确认是否真的修好。

### 3. 应用或丢弃

默认不会改原项目。

运行通过后你有三个常用选择：

- **应用修改**：把这次改动写回原项目。
- **丢弃**：不要这次改动，删除隔离副本。
- **查看完整结果**：看每一轮执行和审查发生了什么。

如果运行失败，优先看右侧的下一步建议。它会告诉你更适合继续、重试，还是调整任务描述和验收命令。

## 控制台怎么用

### 任务设置

你需要填三类信息：

- **项目路径**：要让 Verdikt 修改的本地项目。
- **任务目标**：用自然语言说明要完成什么。
- **验收命令**：比如 `npm test`、`pnpm test`、`npx vitest run`。

复杂任务可以填写 **任务节点**。例如：

```text
诊断: 找出失败测试和根因
修复: 修改实现，不改测试
验收: 跑测试并等待审查 agent 通过
```

任务节点不是必填，但它能让长任务更稳，不容易一上来就乱改。

### 任务工作台

工作台会显示：

- 正在运行的任务
- 排队中的任务
- 历史任务
- 可以继续的任务

你可以直接从工作台打开某次运行，也可以对失败任务重试，对中断任务继续。

### 查看修改

任务通过后，查看修改会告诉你：

- 改了哪些文件
- 每个文件增删了多少
- 最终补丁内容
- 是否有明显需要注意的地方

建议先看查看修改，再决定是否应用。

### 双 agent 详情

点击 **查看完整结果** 后，会打开运行详情页。这里按轮次展示：

- 执行 agent 本轮做了什么
- 审查 agent 本轮发现了什么
- 验收命令是否通过
- 每轮花费和耗时

这就是 Verdikt 防止“没完成却说完成”的主要依据。

## 创建自己的任务

最简单的方式是打开 Web 控制台直接填表。

> **仓库必须是干净的**:提交任务时,如果目标仓库存在未提交的改动,任务会在开始前被直接拒绝(此时不产生任何花费)。原因是隔离副本基于 HEAD 创建——未提交的改动不会被 agent 看到,而且运行通过后的补丁会因为安全校验无法一键应用。先 `git commit` 或 `git stash` 后重试;确实需要强制继续时,可在任务中设置 `"allowDirtyRepo": true`(命令行为 `--allow-dirty`),但通过后的补丁需要手动应用。`--no-worktree` 直改模式不受此限制。

如果你想用任务文件，也可以这样：

```bash
node dist/index.js init fix-login-bug D:\project\my-app
```

然后编辑生成的任务文件：

```json
{
  "id": "fix-login-bug",
  "goal": "修复登录功能：用户名为空时应返回 400，而不是 500。",
  "repoPath": "D:\\project\\my-app",
  "acceptance": {
    "steps": [
      { "id": "test", "command": "npm", "args": ["test"] },
      { "id": "typecheck", "command": "npx", "args": ["tsc", "--noEmit"] }
    ]
  },
  "maxIterations": 5,
  "maxBudgetUsd": 5,
  "integrity": {
    "allowTestChanges": false
  }
}
```

运行：

```bash
node dist/index.js run --task fix-login-bug.task.json
```

## 失败后怎么处理

### 情况 1：任务没通过，但还有明显下一步

点 **继续运行** 或在工作台里点 **重新运行**。

### 情况 2：任务目标写得太模糊

修改任务目标，把完成标准写具体。例如不要写“修复登录”，而是写“当用户名为空时返回 400，并保持现有登录成功流程不变”。

### 情况 3：验收命令本身跑不起来

先在目标项目里手动运行验收命令，确认本机依赖、环境变量和测试命令没问题。

### 情况 4：补丁看起来不安全

不要应用。可以丢弃后改清楚任务约束，再重新运行。

## 常见问题

### 会不会自动改我的原项目？

默认不会。Verdikt 先在隔离副本里改，只有你点 **应用修改** 或运行 `apply` 命令，才会写回原项目。

### 我能同时提交多个任务吗？

可以。控制台会排队运行，一次只跑一个，避免多个 agent 同时改同一个工作区造成混乱。

### 浏览器没有自动打开怎么办？

手动打开终端里显示的地址，默认是：

```text
http://127.0.0.1:3849
```

### 什么时候用“继续”，什么时候用“重试”？

中断但状态还在时用 **继续**。失败后想用同样目标重新跑一遍，用 **重试**。

### 为什么建议先跑示例？

示例能确认你的 Node.js、Git、Claude Code CLI、测试命令和浏览器控制台都能正常协作。示例跑通后，再放到真实项目里会省很多排查时间。

## 建议用法

- 先用小任务验证一遍流程。
- 任务目标写具体，不要只写“优化一下”。
- 验收命令越可靠，Verdikt 越可靠。
- 重要改动先看查看修改，再应用。
- 大任务拆成多个任务节点，效果通常更稳。
## 工作台关闭后会怎样

- 正常关闭：保存正在执行的现场，保留隔离副本，重启后自动恢复队列。
- 点击“停止运行”：按用户取消处理，不会在下次启动时偷偷继续。
- 异常退出：只要已有保存状态，重启后会把任务放回队列。

工作台会显示当前阶段、阶段内尝试次数、最近心跳和恢复原因。长时间没有新日志时，仍会定期更新心跳，避免把仍在运行的任务误判成失效任务。

## 处理高风险确认

当页面显示“等待确认”时，先看风险类别和原因：

- **仅允许这一次**：只批准当前显示的具体动作一次。
- **本次运行持续允许**：本次任务后续遇到同一个具体动作可继续。
- **拒绝并安全停止**：记录拒绝，让运行收尾并保留证据。

命令行等价操作：

```bash
node dist/index.js approve <run-id> --note="已检查发布范围"
node dist/index.js reject <run-id> --note="不允许写生产环境"
```

## 校验证据

在工作台点击 **校验证据**，或运行：

```bash
node dist/index.js verify-evidence <run-id>
```

结果会说明证据是否完整，并列出缺失、被修改或路径异常的文件。

## 重复运行 Benchmark

套件文件可以加入：

```json
{
  "repeats": 3,
  "warmups": 1,
  "failFast": false
}
```

报告会展示每次尝试、整体通过率、中位和最差耗时、波动、不稳定任务、已知花费、花费覆盖样本以及运行环境。
## 规划、时间线和下一轮说明

### 开始前先规划

任务设置里有三种选择：

- **关闭**：直接开始执行。
- **复杂任务自动规划**：目标较长、存在明显风险或涉及架构、认证、迁移等内容时先规划。
- **必须先规划并确认**：先生成方案，等你确认后才允许修改文件。

规划阶段只能查看和搜索项目。方案会保存在运行记录里，后续执行和证据校验都能看到。

### 下一轮补充说明

运行中可以填写“下一轮补充说明”。它不会打断当前步骤，只会在下一轮开始时使用一次。命令行等价操作：

```bash
node dist/index.js note <run-id> "不要改接口，先解决缓存失效"
```

### 完整运行时间线

控制台会按顺序显示工作区准备、规划、每轮执行、验收、审查、确认、补充说明、回退和补丁处理。页面关闭再打开后仍然能继续读取。

## 回到历史轮次和创建新尝试

在控制台输入轮次后：

- **回到这一轮**：把当前可继续任务恢复到该轮，然后从那里继续。
- **从这一轮新建尝试**：保留原任务，创建一个独立的新方向。

命令行等价操作：

```bash
node dist/index.js rewind <run-id> 2
node dist/index.js fork <run-id> 2
```

已完成并生成最终结果的任务不能原地回退，但可以从历史轮次创建新尝试。

## 具体危险操作确认

等待确认时，页面会显示具体命令、使用的工具和运行位置：

- **仅允许这一次**：批准后只能执行一次；再次遇到同一动作还会询问。
- **本次运行持续允许**：本次任务后续遇到同一动作可以继续。
- **拒绝并安全停止**：停止危险动作并保留记录。

命令行可用：

```bash
node dist/index.js approve <run-id> --scope=once
node dist/index.js approve <run-id> --scope=run
node dist/index.js reject <run-id>
```

## 项目检查扩展

任务文件可以加入项目内的 JavaScript 检查脚本：

```json
{
  "hooks": [
    {
      "event": "before_run",
      "script": "scripts/verdikt-check.cjs",
      "timeoutMs": 15000,
      "failureMode": "block"
    },
    {
      "event": "after_judges",
      "script": "scripts/verdikt-check.cjs",
      "failureMode": "warn"
    }
  ]
}
```

可用节点：`before_run`、`after_plan`、`after_executor`、`after_judges`、`before_apply`、`after_run`。脚本必须位于目标项目内，只能是 `.js`、`.cjs` 或 `.mjs`。完整示例见 `examples/advanced.task.json`。

## 提前准备隔离副本

大型项目如果创建隔离副本较慢，可以先运行：

```bash
node dist/index.js warm D:\project\my-app
```

下一次运行只有在项目版本没变、预备副本仍然干净时才会使用它。预备副本被占用时不会被另一个任务抢走。

## 花费显示规则

- `$0.25`：已拿到完整数据。
- `$0.25+`：目前至少花了这些，但数据不完整。
- `未知` 或 `unknown`：没有足够数据，不能当成零。

Benchmark 的平均花费只使用有可靠数值的样本，并显示覆盖了多少个样本。

## 运行记录保存在哪里

所有运行记录默认写入**启动 Verdikt 时所在目录**下的 `.verdikt/`（不是目标项目里）。也就是说，从不同目录启动 `verdikt` 会看到不同的历史记录。

想固定或迁移记录位置，设置环境变量 `VERDIKT_STATE_DIR`：

```powershell
$env:VERDIKT_STATE_DIR = "D:\verdikt-data"
node dist/index.js app
```

隔离副本、队列（queue.json）、锁文件都保存在同一个状态目录下，整体拷贝该目录即可迁移全部历史。
